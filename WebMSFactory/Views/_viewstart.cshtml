@{
	if (!Request.Url.ToString().Contains("Login") && Session["login"] == null && !Response.IsRequestBeingRedirected)
	{ 
		Response.Redirect("/Login/Index");
		return;
	}

	Layout = "~/Views/Shared/_LayoutPage.cshtml";
}